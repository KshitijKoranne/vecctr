import { GeneratorsGallery } from "@/components/landing/GeneratorsGallery";

export const metadata = {
  title: "Generators — Vecctr",
  description: "Choose from 10 unique SVG generators. Free to use, no signup required.",
};

export default function GeneratorsPage() {
  return <GeneratorsGallery />;
}
